---
title: "Authentication API"
description: "User authentication and profile management endpoints"
icon: "key"
api: "POST /api/auth/register"
---

## Register

<ParamField path="email" type="string" required>
  User's email address
</ParamField>

<ParamField path="password" type="string" required>
  Strong password (min 8 characters)
</ParamField>

<ParamField path="first_name" type="string">
  User's first name
</ParamField>

<ParamField path="last_name" type="string">
  User's last name
</ParamField>

```bash Example Request
curl -X POST http://localhost:5000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "user@example.com",
    "password": "SecurePass123!",
    "first_name": "John",
    "last_name": "Doe"
  }'
```

```json Example Response
{
  "message": "User registered successfully",
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "user": {
    "id": 1,
    "email": "user@example.com",
    "role": "editor"
  }
}
```

## Login

<api method="POST" path="/api/auth/login" />

<ParamField path="email" type="string" required>
  User's email
</ParamField>

<ParamField path="password" type="string" required>
  User's password
</ParamField>

```bash Example Request
curl -X POST http://localhost:5000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "user@example.com",
    "password": "SecurePass123!"
  }'
```

## Get Profile

<api method="GET" path="/api/auth/profile" auth="bearer" />

```bash Example Request
curl -X GET http://localhost:5000/api/auth/profile \
  -H "Authorization: Bearer YOUR_TOKEN"
```

## Update Profile

<api method="PUT" path="/api/auth/profile" auth="bearer" />

## Change Password

<api method="PUT" path="/api/auth/change-password" auth="bearer" />
